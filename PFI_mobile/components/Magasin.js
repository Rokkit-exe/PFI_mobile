import React from 'react';
import { View, StyleSheet } from 'react-native';

function Magasin(props) {
    return (
        <View style={styles.container}>Magasin</View>
    );
}

const styles = StyleSheet.create({
    container: {
    }
})

export default Magasin;
